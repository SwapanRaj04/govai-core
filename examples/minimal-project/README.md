# GovAI Core - Minimal Project Example

This is a minimal working example demonstrating how to integrate and use the `aigov_audit` policy enforcement engine in a Rust application.

## Overview
1. **Case A (Invalid Payload):** Catches expected `schema_invalid` violations.
2. **Case B (Compliant Payload):** Demonstrates full verification success.

## Running the Example
```bash
cargo run
```
